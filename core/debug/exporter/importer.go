package exporter

import (
	"context"
	"path/filepath"
	"strings"
)

func ImportTree(ctx context.Context, path string) error {
	targetDir := strings.TrimSuffix(sourceZip, filepath.Ext(sourceZip))
	return nil
}
