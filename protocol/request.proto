syntax="proto3";

// TODO
message DataMessage {

    enum Entity {
        DOCUMENT = 0;
        BLOCK = 1;
    }

    enum Type {
        REQUEST = 0;
        RESPONSE = 1;
    }

    enum Status {
        SUCCESS = 0;
        FAILURE = 1;
        NEUTRAL = 2;
    }
}